package com.eventhub.event.entity;

public enum SeatingType {

    THEATRE,     // Rows & columns (cinema style)
    STADIUM,     // curved / tier-based
    OPEN         // no fixed seats (future: general entry)
}